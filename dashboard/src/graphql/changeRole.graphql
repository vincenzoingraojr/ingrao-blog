mutation changeRole(
    $id: Int!
    $role: String!
) {
    changeRole(
        id: $id
        role: $role
    ) {
        status
        errors {
            field
            message
        }
    }
}