mutation passwordSetup(
    $token: String!
    $confirmPassword: String!
    $password: String!
) {
    passwordSetup(
        token: $token
        confirmPassword: $confirmPassword
        password: $password
    ) {
        status
        errors {
            field
            message
        }
    }
}